# DRIFT.md — Plan vs. Reality

> Auto-generated by plan conformance tests. Re-run: `pytest tests/plan/ -v`

## Status (2026-07-06)

**102 commitments checked, 0 failures.**

## Commitments Verified

### Architecture Boundaries (14 checks)
- `environment/` never imports `sandbox/`, `intelligence/`, or `search/`
- `sandbox/` never imports `intelligence/` or `search/`
- `reward/` never imports `runtime/`, `sandbox/`, or `environment/`
- `search/` never imports `sandbox/` or `intelligence/`
- `runtime/` never imports `intelligence/` or `search/`
- `intelligence/` never imports `runtime/` or `search/`

### Feature Completeness (Phase 1 — 37 checks)
- All 8 Phase 1 modules exist (ir, runtime, environment, sandbox, trace, reward, search, intelligence)
- Phase 2/3 modules do NOT exist (skills, memory, compiler)
- UPIR has entry, nodes, edges, harness_table, skill_table fields
- Edge has from_, to, kind with alias support
- 7 node types present: Observe, Act, Think, Branch, SkillCall, HarnessCall, Phi
- Harness has kind, code, effects, guard_policy
- Environment Protocol, ToolEnvironment, GameEnvironment exist
- Sandbox guardrails, workspace, harness_runner exist
- Reward has task_success, efficiency, safety, skill_gain, legality
- CriticOutput has failure_clusters, legality_violations, inefficiency_patterns
- ThompsonTreeSearch, Branch, thompson_sample, update_posterior exist

### Dependency Budget (5 checks)
- Exactly 2 runtime deps: openai, pydantic
- No heavy ML deps (torch, tensorflow, etc.)
- Python >= 3.12

### Config (6 checks)
- config.py exists with VM_MAX_STEPS, SANDBOX_MAX_RUNTIME_MS
- Thompson defaults (iterations, failures, prior alpha/beta)
- Reward weights sum to ~1.0

### Conventions (20 checks)
- ruff line-length = 100, target py312
- mypy strict mode with pydantic plugin
- Pre-commit: ruff, mypy, gitleaks, bandit
- CI: lint, test, security jobs
- Test directory structure matches source modules

## Deliberate Plan Changes

These are known deviations from PLAN.md where the implementation diverged intentionally:

| What PLAN said | What we decided | Date | Reason |
|---|---|---|---|
| `validator.py` for edge consistency | Validation in UPIR Pydantic model_validator | 2026-07-06 | Thin wrapper added no value; Pydantic handles it |
| Separate `reward.py`, `value.py` | Unified `scorer.py` with value() | 2026-07-06 | Single file simpler for Phase 1; split later if needed |
| `emitter.py` in trace/ | Trace emission inline in VM | 2026-07-06 | VM emits trace events directly; no separate emitter needed yet |
| `rollout.py` in search/ | Rollout logic in ThompsonTreeSearch.run() | 2026-07-06 | Single rollout function inside search; extract when parallelism needed |
| CI runs bandit directly | CI runs via security-scan.sh | 2026-07-06 | Centralized scan script handles bandit + shellcheck + pip-audit |
| ThompsonWeights in config | Weights hardcoded in value() | 2026-07-06 | Simplest path for Phase 1; configurable weights deferred |
| Python 3.9 in plan's pyproject section | Python 3.12+ everywhere | 2026-07-06 | Upgraded to 3.12 for modern Python features |

## Areas Too Vague to Check

- **Test counts per slice**: Plan lists specific test names but not counts; conformance checks module existence, not coverage.
- **Determinism guarantees**: Plan says "deterministic given same seed" but conformance tests don't verify trace replay.
- **Guardrail enforcement depth**: Plan describes guardrails in detail but conformance only checks module existence, not runtime behavior.
- **Value function weights**: Plan's ThompsonWeights differ from config.py values — both are "correct" depending on interpretation.
